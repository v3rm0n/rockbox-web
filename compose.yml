services:
  crate:
    build: .
    container_name: crate
    ports:
      - "3000:3000"
    volumes:
      - crate-data:/data
      - /Volumes/music:/library:ro
      - /Volumes/IPOD:/player
    environment:
      - ORIGIN=http://localhost:3000
    restart: unless-stopped

volumes:
  crate-data:
